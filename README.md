# pomodoro-timer-CLI
A productivity timer based on the Pomodoro technique. Runs work sessions and breaks with a live Rich progress bar countdown, plays a terminal beep when each interval ends, and logs every session to a JSON file so you can track your focus over time
